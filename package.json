{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.26.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "preinstall": "rm -f package-lock.json yarn.lock",
    "build": "corepack enable && corepack prepare pnpm@10.26.1 --activate && pnpm install --prod=false && pnpm --filter @workspace/jivara run build",
    "start": "pnpm --filter @workspace/jivara run start",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
